name: distancelearning_mobile
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  ext_storage: ^1.0.3
  fluttertoast: ^8.0.7
  path_provider: ^2.0.2
  permission_handler: ^8.0.0+2
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.3

flutter:
  uses-material-design: true
